# Oni · hyprlock — black screen, mint clock
background {
    monitor =
    color = rgb(000000)
}
label {
    monitor =
    text = $TIME
    color = rgb(5fe0c0)
    font_size = 72
    font_family = Chakra Petch
    position = 0, 80
    halign = center
    valign = center
}
input-field {
    monitor =
    size = 260, 44
    outline_thickness = 1
    inner_color = rgb(050808)
    outer_color = rgb(2e8f79)
    font_color = rgb(c9d2cf)
    placeholder_text = ...
    position = 0, -40
    halign = center
    valign = center
}
